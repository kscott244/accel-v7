$TYPES_CONTENT

// ── Account Memory ──
// Re-exported from lib/accountMemory for convenience in components.
// Full implementation lives in src/lib/accountMemory.ts.
export type { AccountMemory, RelationshipState } from "@/lib/accountMemory";

// ── App Event ──
// Re-exported from lib/eventLog for convenience in components.
// Full implementation lives in src/lib/eventLog.ts.
export type { AppEvent, AppEventType } from "@/lib/eventLog";
